# {システム名}：情報定義

## 1. エンティティ定義

### 1-0) 共通項目（全エンティティ共通）

| 属性名 | データ型 | 必須 | 説明 | メンテルール |
| --- | --- | --- | --- | --- |
| 作成日時 (createdAt) | 日時(ISO8601) | 必須 | レコード作成日時 | 新規作成時に自動設定。更新時は変更しない |
| 更新日時 (updatedAt) | 日時(ISO8601) | 必須 | レコード更新日時 | 更新時に上書き |

### 1-1) {エンティティ名}

- **概要**：
- **主キー**：
- **業務キー（ユニーク）**：

| 属性名 | データ型 | 主キー | 業務キー | 必須 | 説明 | 制約 / 備考 | 関連先 |
| --- | --- | :---: | :---: | --- | --- | --- | --- |
|  |  |  |  |  |  |  |  |

> 主キー・業務キーは該当する属性に `○`（複合なら各属性に `○`）。上の箇条書きと必ず一致させる。

## 2. リレーション定義（総覧）

- {エンティティA} **1 — N** {エンティティB}（FK: ）

**主なユニーク制約**

- 

## 3. 業務ルール・バリデーション（整合性担保）

### 3-1. 業務ルール

- 

### 3-2. バリデーション

#### V-1 {ルール名}

- 対象：{エンティティ.属性} ／ 層：単項目 / 項目間 / エンティティ間
- 条件：
- タイミング：登録時 / 更新時 / 状態遷移時 / バッチ時 ／ 違反時：拒否 / 警告 / 補正
- 担保場所：DB制約 or アプリ業務ルール（→ §4）

## 4. 物理データ設計・実装方針（任意）

- DB選定（RDB / NoSQL）と設計思想：
- 整合性の担保場所：DBが宣言的に守るもの（一意・FK・値域）／アプリ集計で判定するもの（合計・上限）

## 5. 物理エンティティ設計詳細（任意）

（RDB版：テーブル・FK・UNIQUE・インデックス ／ NoSQL版：アクセスパターン一覧 → キー設計・非正規化・整合性担保）
